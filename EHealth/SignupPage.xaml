<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:syncfusion="clr-namespace:Syncfusion.Maui.Calendar;assembly=Syncfusion.Maui.Calendar"
             x:Class="EHealthApp.AppointmentsPage"
             Title="Programări">
	<VerticalStackLayout Padding="16" Spacing="12">
		<Label Text="Calendar programări" FontSize="22" HorizontalOptions="Center"/>
		<syncfusion:SfCalendar x:Name="MedicalCalendar"
                               View="Month"
                               ShowTodayButton="True"
                               SelectionChanged="MedicalCalendar_SelectionChanged"
                               HeightRequest="350"/>
		<HorizontalStackLayout Spacing="10">
			<Button Text="Adaugă programare"
                    Clicked="OnAddAppointmentClicked"
                    HorizontalOptions="FillAndExpand"/>
			<Button Text="Vezi toate programările"
                    Clicked="OnViewAllAppointmentsClicked"
                    HorizontalOptions="FillAndExpand"/>
		</HorizontalStackLayout>
		<Label Text="Programările zilei selectate:" FontSize="18" Margin="0,12,0,0"/>
		<CollectionView x:Name="AppointmentsList"
                        HeightRequest="130">
			<CollectionView.ItemTemplate>
				<DataTemplate>
					<Frame Margin="4" Padding="10" BackgroundColor="#f5f5f5" CornerRadius="8">
						<VerticalStackLayout>
							<Label Text="{Binding Ora}" FontSize="16" TextColor="#576cbc"/>
							<Label Text="{Binding Descriere}" FontSize="14"/>
						</VerticalStackLayout>
					</Frame>
				</DataTemplate>
			</CollectionView.ItemTemplate>
		</CollectionView>
	</VerticalStackLayout>
</ContentPage>